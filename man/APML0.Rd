% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14-APML0.R
\name{APML0}
\alias{APML0}
\title{Augmented and Penalized Minimization with L0 Regularization}
\usage{
APML0(
  x,
  y,
  family = c("gaussian", "binomial", "cox"),
  penalty = c("Lasso", "Enet", "Net"),
  Omega = NULL,
  alpha = 1,
  lambda = NULL,
  nlambda = 50,
  rlambda = NULL,
  wbeta = rep(1, ncol(x)),
  sgn = rep(1, ncol(x)),
  nfolds = 1,
  foldid = NULL,
  ill = TRUE,
  iL0 = TRUE,
  icutB = FALSE,
  ncutB = 10,
  ifast = TRUE,
  isd = FALSE,
  iysd = FALSE,
  ifastr = TRUE,
  keep.beta = FALSE,
  thresh = 1e-06,
  maxit = 1e+05,
  threshC = 1e-05,
  maxitC = 100,
  threshP = 1e-05
)
}
\arguments{
\item{x}{Input matrix of features (n x p)}

\item{y}{Response vector/matrix:
- For Gaussian: numeric vector
- For binomial: binary vector (0/1)
- For Cox: matrix with columns "time" and "status"}

\item{family}{Type of regression model: "gaussian", "binomial", or "cox"}

\item{penalty}{Type of penalty: "Lasso" (L1), "Enet" (L1 + L2), or "Net" (L1 + Laplacian)}

\item{Omega}{Optional penalty matrix for network regularization (for "Net" penalty)}

\item{alpha}{Elastic net mixing parameter (0 <= alpha <= 1).
alpha=1 for Lasso, alpha=0 for Ridge}

\item{lambda}{Regularization parameter sequence. If NULL, automatically generated}

\item{nlambda}{Number of lambda values to generate (default: 50)}

\item{rlambda}{Ratio of smallest to largest lambda (default: adaptive)}

\item{wbeta}{Adaptive weights for coefficients (default: all 1)}

\item{sgn}{Sign constraints for coefficients (default: all 1)}

\item{nfolds}{Number of folds for cross-validation (default: 1)}

\item{foldid}{Optional vector specifying fold membership for each observation}

\item{ill}{Whether to use log-likelihood for model selection (default: TRUE)}

\item{iL0}{Whether to use L0 regularization (default: TRUE)}

\item{icutB}{Whether to use coefficient thresholding (default: FALSE)}

\item{ncutB}{Number of coefficient cuts for thresholding (default: 10)}

\item{ifast}{Whether to use fast algorithm (default: TRUE)}

\item{isd}{Whether to standardize features (default: FALSE)}

\item{iysd}{Whether to standardize response (Gaussian only, default: FALSE)}

\item{ifastr}{Whether to use fast cross-validation (default: TRUE)}

\item{keep.beta}{Whether to keep all coefficient paths (default: FALSE)}

\item{thresh}{Convergence threshold for optimization (default: 1e-6)}

\item{maxit}{Maximum number of iterations (default: 1e+5)}

\item{threshC}{Convergence threshold for coordinate descent (default: 1e-5)}

\item{maxitC}{Maximum iterations for coordinate descent (default: 1e+2)}

\item{threshP}{Convergence threshold for proximal operator (default: 1e-5)}
}
\value{
An object of class "APML0" containing:
\item{Beta}{Estimated coefficients}
\item{fit}{Model fit information including lambda path and performance metrics}
\item{family}{Type of regression model}
\item{penalty}{Type of penalty used}
\item{adaptive}{Whether adaptive penalties were used}
\item{flag}{Convergence flags}
}
\description{
Fits regularized regression models with L0 penalty for feature selection.
Supports Gaussian, binomial, and Cox regression with various penalty types
including Lasso, Elastic Net, and Network regularization.
}
\examples{
\dontrun{
# Gaussian regression example
set.seed(123)
x <- matrix(rnorm(100 * 20), 100, 20)
y <- rnorm(100)

# Fit Lasso model with L0 regularization
fit <- APML0(x, y, family = "gaussian", penalty = "Lasso", nlambda = 30)

# Cox regression example
library(survival)
y_surv <- cbind(time = rexp(100), status = rbinom(100, 1, 0.5))
fit_cox <- APML0(x, y_surv, family = "cox", penalty = "Enet", alpha = 0.5)
}

}
\seealso{
[print.APML0()] for printing results, [LmL0()], [LogL0()], [CoxL0()]
}
